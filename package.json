{
  "name": "circle-distance",
  "version": "1.0.0",
  "description": "calculate distance",
  "main": "index.js",
  "scripts": {
    "clone": "node index.js --script=clone",
    "distance": "node index.js --script=distance",
    "test": "jest --config ./jest.config.json",
    "test:watch": "npm run test -- --watchAll"
  },
  "author": "vivek thumar",
  "license": "ISC",
  "dependencies": {
    "arg": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
